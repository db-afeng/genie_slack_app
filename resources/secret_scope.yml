# yaml-language-server: $schema=../bundle_schema.json
resources:
  secret_scopes:
    genie_slack_secret_scope:
      name: genie-slack-secret-scope
      permissions:
      - level: READ
        service_principal_name: ${resources.apps.genie-slack-app.service_principal_name}
