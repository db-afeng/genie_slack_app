resources:
  database_instances:
    genie_slack_lakebase:
      name: genie-slck-${workspace.current_user.domain_friendly_name}
      capacity: CU_1
  database_catalogs:
    genie_slack_database:
      database_instance_name: ${resources.database_instances.genie_slack_lakebase.name}
      name: genie-slck-${workspace.current_user.domain_friendly_name}
      database_name: genie-slck-${workspace.current_user.domain_friendly_name}
      create_database_if_not_exists: true
